<%= stylesheet_link_tag "offers.css"  %>

<div class="content">
  <div class="content-header">
    <p style="color: green"><%= notice %></p>
    <h2 class="title"><%= "#{current_user.name}" %><span class="total"> ( <%= current_user.role.humanize %> )</span></h2>
  </div>
  <div class="content-body home">
    <%= render :partial => current_user.role.underscore %>
  </div>
</div>